Global Function
- parseInt()-string to parseInt
- isNaN() - is not a number
- isFinite() - whether the value is a finite number
- decodeURI() and encodeURI() - URI COmponents
- eval() - evaluate a string as JS
- alert(), prompt(), confirm() -  

Arrays
- dynamic entities in JS.
- We can create array in JS by two methods. By using
Array Literal and Built in COnstruuctor.
- var s = [item1,item2]
    var s = new Array(item1,item2)
- size of the array is determined by the number of
values while initialization
- var n = [10, 20, , 30] - 3rd element is undefined

- array_name.length - to return the length of the array
- array_name.push(item)

- adding elements at higher indexes create undefined values
- shift() - to pop from beginning
- unshift() - to add at the begginning
- delete arr[0] - to delete a element from a specific position
                - it leaves the position a hole

- arr.splice(indexOfWheretoAdd,howManyElementstoRemove,item1,item2,item3)

- arr1.concat(arr2) - to merge Arrays
                    - can take any number of arguments
                    - 
